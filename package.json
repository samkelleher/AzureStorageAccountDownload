{
  "name": "azure-container-sync",
  "version": "1.2.0",
  "description": "A utility to download entire Azure containers to your machine.",
  "module": "index.mjs",
  "scripts": {
    "start": "DEBUG=*,-follow-redirects node --experimental-modules --no-warnings -r ./node_modules/dotenv/config.js index.mjs",
    "lint": "eslint index.mjs --ext=mjs"
  },
  "private": true,
  "engines": {
    "node": ">=12"
  },
  "author": "Sam Kelleher",
  "license": "ISC",
  "dependencies": {
    "@azure/storage-blob": "^10.3.0",
    "debug": "^4.1.1",
    "dotenv": "^7.0.0",
    "envalid": "^5.0.0"
  },
  "devDependencies": {
    "eslint": "^5.16.0"
  }
}
